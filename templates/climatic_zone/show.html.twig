{% extends 'base.html.twig' %}

{% block title %}ClimaticZone{% endblock %}

{% block body %}
    <h1>ClimaticZone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ climatic_zone.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ climatic_zone.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('climatic_zone_index') }}">back to list</a>

    <a href="{{ path('climatic_zone_edit', {'id': climatic_zone.id}) }}">edit</a>

    {{ include('climatic_zone/_delete_form.html.twig') }}
{% endblock %}
