{% extends 'base.html.twig' %}

{% block title %}ClimaticZone index{% endblock %}

{% block body %}
    <h1>ClimaticZone index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for climatic_zone in climatic_zones %}
            <tr>
                <td>{{ climatic_zone.id }}</td>
                <td>{{ climatic_zone.name }}</td>
                <td>
                    <a href="{{ path('climatic_zone_show', {'id': climatic_zone.id}) }}">show</a>
                    <a href="{{ path('climatic_zone_edit', {'id': climatic_zone.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('climatic_zone_new') }}">Create new</a>
{% endblock %}
