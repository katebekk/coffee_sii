{% extends 'base.html.twig' %}

{% block title %}CountFeature{% endblock %}

{% block body %}
    <h1>CountFeature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ count_feature.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ count_feature.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('count_feature_index') }}">back to list</a>

    <a href="{{ path('count_feature_edit', {'id': count_feature.id}) }}">edit</a>

    {{ include('count_feature/_delete_form.html.twig') }}
{% endblock %}
