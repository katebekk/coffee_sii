{% extends 'base.html.twig' %}

{% block title %}CoffeeSort{% endblock %}

{% block body %}
    <h1>CoffeeSort</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coffee_sort.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ coffee_sort.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('coffee_sort_index') }}">back to list</a>

    <a href="{{ path('coffee_sort_edit', {'id': coffee_sort.id}) }}">edit</a>

    {{ include('coffee_sort/_delete_form.html.twig') }}
{% endblock %}
