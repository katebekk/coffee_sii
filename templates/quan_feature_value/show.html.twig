{% extends 'base.html.twig' %}

{% block title %}QuanFeatureValue{% endblock %}

{% block body %}
    <h1>QuanFeatureValue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quan_feature_value.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quan_feature_value_index') }}">back to list</a>

    <a href="{{ path('quan_feature_value_edit', {'id': quan_feature_value.id}) }}">edit</a>

    {{ include('quan_feature_value/_delete_form.html.twig') }}
{% endblock %}
