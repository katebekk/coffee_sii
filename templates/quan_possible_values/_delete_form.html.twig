<form method="post" action="{{ path('quan_possible_values_delete', {'id': possible_value.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ possible_value.id) }}">
    <button class="btn">X</button>
</form>
