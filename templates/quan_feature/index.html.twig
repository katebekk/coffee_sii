{% extends 'base.html.twig' %}

{% block title %}QuanFeature index{% endblock %}

{% block body %}
    <h1>QuanFeature index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for quan_feature in quan_features %}
            <tr>
                <td>{{ quan_feature.id }}</td>
                <td>{{ quan_feature.name }}</td>
                <td>
                    <a href="{{ path('quan_feature_show', {'id': quan_feature.id}) }}">show</a>
                    <a href="{{ path('quan_feature_edit', {'id': quan_feature.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('quan_feature_new') }}">Create new</a>
{% endblock %}
