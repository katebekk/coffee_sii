{% extends 'base.html.twig' %}

{% block title %}QuanFeature{% endblock %}

{% block body %}
    <h1>QuanFeature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quan_feature.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quan_feature.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quan_feature_index') }}">back to list</a>

    <a href="{{ path('quan_feature_edit', {'id': quan_feature.id}) }}">edit</a>

    {{ include('quan_feature/_delete_form.html.twig') }}
{% endblock %}
