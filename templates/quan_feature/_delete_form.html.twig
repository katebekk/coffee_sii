<form method="post" action="{{ path('quan_feature_delete', {'id': quan_feature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quan_feature.id) }}">
    <button class="btn">X</button>
</form>
