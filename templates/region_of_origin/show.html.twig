{% extends 'base.html.twig' %}

{% block title %}RegionOfOrigin{% endblock %}

{% block body %}
    <h1>RegionOfOrigin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ region_of_origin.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ region_of_origin.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('region_of_origin_index') }}">back to list</a>

    <a href="{{ path('region_of_origin_edit', {'id': region_of_origin.id}) }}">edit</a>

    {{ include('region_of_origin/_delete_form.html.twig') }}
{% endblock %}
